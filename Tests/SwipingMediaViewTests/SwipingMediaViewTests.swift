import XCTest
@testable import SwipingMediaView

final class SwipingMediaViewTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(SwipingMediaView().text, "Hello, World!")
    }
}
